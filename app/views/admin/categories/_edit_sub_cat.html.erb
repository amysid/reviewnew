      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal">&times;</button>
       
        </div>
        <div class="modal-body">
          <div class="addbody text-left">
           <%= form_for :sub_categories, url:  update_sub_category_admin_categories_path  do |f| %>


                 <div class="form-group">
                <label for="">Sub Category Name<span class="required">*</span> </label>
                  <%= f.text_field :sub_category_name,  class: "form-control custom-control",  placeholder: "Sub Categories Name"%>
                 </div>
               <%#= f.hidden_field :id, :value => '@sub.id'  %>
         <%= f.hidden_field :category_id, :value =>  @sub_categories.category_id %>
              <input type = "hidden" name = "subcat_id" value = "<%=@sub_categories.id%>">
               
       
        <div class="modal-footer text-center">
          <%= submit_tag 'Update', :class => "btn btn-primary btn-md"%>
        </div>
         </div>
        <%end%>
         </div>
      </div>